#include <tunables/global>

profile homeii flags=(attach_disconnected,mediate_deleted) {
  #include <abstractions/base>
  #include <abstractions/python>
  network inet stream,
  network inet6 stream,
  /opt/homeii/** r,
  /usr/bin/python3 ix,
  /usr/lib/python3.*/** mr,
  /data/** rwk,
  /config/** rwk,
  /run/s6/** rw,
}

